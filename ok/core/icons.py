"""Semantic icon identifiers understood by optional UI adapters."""

from enum import StrEnum


class Icon(StrEnum):
    APPLICATION = "application"
    GAME = "game"
    PEOPLE = "people"
    RINGER = "ringer"
    SYNC = "sync"
    UPDATE = "update"
